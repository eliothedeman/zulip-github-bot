[project]
name = "zulip-github-bot"
version = "0.1.0"
description = "A Zulip bot that creates GitHub issues from chat messages"
requires-python = ">=3.10"
dependencies = [
    "zulip>=0.9.0",
    "PyGithub>=2.1.0",
]

[project.scripts]
zulip-github-bot = "zulip_github_bot.bot:main"
